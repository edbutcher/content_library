import React from 'react';

import Nav from '../Nav';
import './style.scss';

const Header = () => {
    return(
        <header id="header">
            <Nav />
        </header>
    );
};

export default Header;