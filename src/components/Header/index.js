import React from 'react';

import Nav from '../Nav';
import './style.css';

const Header = () => {
    return(
        <header id="header">
            <Nav />
        </header>
    );
};

export default Header;